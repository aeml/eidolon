# Rendered ranked team check — prepared, not executed

Remaining scope is the actual client presentation of ranked team rounds and
personal outcomes. Keep the accepted four-socket combat/results/restart and
four-class practice cadence evidence; do not recreate their full matrices.

Prepared opt-in route `arena-team-render` reuses the existing isolated wrapper,
ordinary role-gear catalog and prepared-character registration helper. Four
distinct level30 Wizards wear legal Brilliant Rare/Uncommon gear. This is not
earned leveling or four-class ranked balance proof. Two ordinary UI invitations
form the two parties; each leader queues using the actual ranked2v2 control.
Targets are selected through normal pointer input, without injected combat
messages, damage, deaths, match state or timers. All four must land real damage,
observe at least two rounds and the same first-to-two outcome. The review captures
combat, the round panel, and each personal result; an ordinary re-login retains
the same result. No repeated crash/reward/season matrix is added.

Bound: one run, zero retries, ten minutes including preparation, four-minute
combat deadline. Failure preserves bounded client diagnostics and world-only
screenshots. No automatic restart or weakened assertion. Invocation uses the
existing disposable setup with `EIDOLON_ISOLATED_QA_ROUTE=arena-team-render` and
unused local ports; never overlap the active five-browser Tidestar run.

Worktree: `/tmp/eidolon-arena-review-20260919-LlO1tu`, branch
`work/arena-review-20260919`, based on4095f51d. The running raid's main worktree
and launcher were not edited. Syntax/lint, shell syntax, diff checks and native
Playwright discovery pass. **No browser execution or new arena acceptance yet.**
Merge only after preserving/reviewing the active raid's terminal artifacts.
