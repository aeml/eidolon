# Alpha1.0.45 release integration — pending

Base2244325 retains the compact phone status panel and exact44 ancestry.
Requested regeneration candidate2580ac2 is cherry-picked asbe29e2e. The verified
nightly isolation files are copied exactly from4bfd205 without importing root's
unreleased50 runtime or stale release ledger. Existing queue refs are preserved.
Actual45 patch notes now describe the0.01 final rate, old formula/tooltip
discrepancy, fractional ticks, unchanged active recovery, compact status panel
and owned soak isolation. Package/lock/login/runtime/server/workflow identities
remain45; this is not published yet.

The standalone regeneration candidate passes219 client suites/3225 tests and
full server race checks. These are component/base50 results, not yet proof of
this combined45 release. Run combined checks and the existing fresh opening/
collection route under the new rate. This build still uses the old progression
curve and15-chapter catalog; it is not the31-chapter balance/story expansion.
Do not relax earned deaths or silently restore regeneration to make QA pass.

Push only after44 CI34187405437 passes all eight jobs and fresh exact public
identity checks. Publish this explicit release branch, never root master. Later
46–50 candidates must inherit45's regeneration and operational fixes before
their own sequential publication; old branches remain recoverable references.

Combined source762d7b2: full server race40404 passed (see actual log for package
durations); fresh Wizard18393 **PASS /one /4.7m**, scan/owned cleanup pass.
Replicated starter rates were0.10000000149, matching0.1 within protobuf precision.
Opening500XP/100gold completed59s, level5, no deaths/two retreats. Collection
earned four Seeds after eight observed target deaths, no deaths,216s; explicit
8000XP/100gold turn-in reached17, reconnect retained it, and the level30 dungeon
gate remained closed. These old rewards still require the planned balance pass;
the run proves the current route works under the requested regeneration change.

Full client22164 initially failed one stale expected45 patch-note title;215
suites/3192 tests passed. The title expectation is corrected, and the same test
now requires the new recovery/soak headings and actual0.5→0.01 wording without
removing status or prior-version history checks. A corrected full client run is
required. No runtime or earned-play assertion changed after the passing runs.
