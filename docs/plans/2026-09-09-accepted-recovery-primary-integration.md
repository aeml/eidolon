# Accepted town recovery ancestry in the next-version branch

Recovery58 ef9a638 completed all ten CI jobs and final native live recovery QA
at15:28:04UTC. Fresh public client/login/runtime/backend identities and actual
production backup integrity/exact previous-image load were verified22:08–22:09.
This merge imports that accepted ancestry into primary941ecf6; it is not a new
release or permission to publish the extra story/curve/visual work as58.

Seven conflicts were reviewed. The QA runner retains the expanded story-only
readiness route and per-stage timing, adds the accepted native rest journey,
expiry and party stage, and preserves focused suffix/all-rest replays. The gate
harness now verifies36 required commands and failure/cleanup at each stage.
The Dockerfile keeps release ARGs after dependency layers while using inherited
58 metadata. Earned preparation/crowd controls/observer idempotence and normal
rest policy survive. The opening story preserves its exact manual payouts,
investigations, hunts and checkpoint policy. The newly merged phone stash
projection and bounded ordinary loot setup remain intact.

Removed duplicate rest-policy import and duplicate pointer-observer block created
by automatic merge; neither removes a gameplay guard. No server damage, reward,
drop or story-content decision is made in this merge.

Focused27848 passed313tests/8suites2.664s plus lint and shell syntax. Source
includes approved deployment-image retention and confidential build-context
checks. Fullclient26781 on51704bd passed277suites3946tests128.14s and lint;
fullGo-race90945 passed (root22.394s/game318.255s/database1.097s). Logs
`/tmp/eidolon-primary-accepted58-full-{client,lint,server}.log`. Server files stayed
unchanged while later client-only bag-policy files were prepared. Actual integrated
browser proof remains required, and subsequent client edits need a new full run.
Do not promote the separate aura batching or CI partition prototypes implicitly.

The latest prior story run11948 FAILED14.6m: the40kill quest/manual payout and
saved state passed469s,0deaths14town stops, but it left25/25inventory slots full.
Collection then stayed0/8 for42selected-target death observations. Viewed actual
failure screenshot shows INVENTORY FULL and Memory Seeds on the ground. Archive
`/tmp/eidolon-story-collection-inventory-failure-OkOJvh`, scanner0 and exact owned
services removed by normal cleanup. This is not missing drops or a reason to
grant fragments. Add explicit ordinary earned bag management and inventory failure
receipts before the next balance playthrough. Milestone training/fullEarth/party
readiness remain unproved.
