# Nearby story inspection after earned balance failure

Fresh candidate **a14351b /80057 FAILED /4.4m** at the new-growth hover check.
Opening completed at level2 with100XP/100gold, zero deaths/50s. Diary's200XP
reached level3. Fifteen observed collection-target deaths yielded eight Seeds,
zero deaths and level4. Five actual equipment drops remained unsold,130gold
vendor value; gold271→601 during collection combat. The ready screen visibly
quoted50gold/1200XP. Image inspected; archived at
`/tmp/eidolon-balanced-earth-failed-AqCuyA/earned-collection-ready.png`.

Failure log `/tmp/eidolon-reward-budget-fresh-collection-prepared.log` shows
new_growth's mesh active/in cache at distance≈3, with Skeleton-297 and35 ahead
of the prop in the raycast. Clearing the approach had not prevented subsequent
hostiles from overlapping it. Do not call this an earned full-route pass.

New deliberate **E** input requests a nearby accepted Chronicle discovery using
the existing server-confirmed inspection message. It does not change hostile
raycast priority, grant credit locally, complete quests, move the character,
extend the5-unit range, suppress enemy damage or make the player invulnerable.
Unrecorded evidence is preferred; unavailable prerequisite sites are excluded.
Typing, modified keys and held-key repeats do not request inspection. Existing
phone USE remains unchanged. Help and hovered discovery text explain the key.

Focused client **75846 PASS /40 tests /3.490s**; expanded input/phone/Chronicle
regression **25658 PASS /74 tests /six suites /2.934s**. Full lint **93843 PASS**,
diff check passes. The fresh route retains a real pointer inspection for the
diary and uses native E for the scar records, with unchanged server-acknowledged
journal/mask/manual-turn-in checks. Full client and real earned rerun remain
required; this code is not release-packaged.
