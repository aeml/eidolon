# Iron Fortress native acceptance route — pending execution

Built on6e752e8f without changing its active hosted rehearsal or release65.
The new `fortress-mastery` route uses a dedicated allowlisted Fighter and is
part of the full timed 1.1 predeploy sequence after Guardian Roar. Automatic
retries are disabled. It is a prepared functional test, not earned progression.

The route requires ordinary phone controls to select the Fighter specialization,
cast a30-second rank-zero baseline, purchase all five FTR_07 ranks one point at
a time, read the corrected talent description, and cast36-second protection.
It checks real accepted-cast mana, authoritative duration snapshots, a visible
High-quality attached effect, and a displayed countdown matching the client
timer. Protection and its badge must remain beyond the old30-second deadline,
then disappear through natural server expiry. Well Rested must remain visible.

After ordinary Extended rune selection, a fresh document login must preserve
the five purchased ranks, point balance and rune. A landscape/Low-quality cast
must last54seconds, remain beyond45seconds and expire normally. Existing
level/readiness commands prepare only the disposable fixture; no direct rank,
timer, buff or clock assignment is used. Wait for any prior buff to expire before
requesting readiness. Full test budget300s includes real-time expiry waits.

The serialized observer forwards original messages and returns/errors unchanged,
installs once per document, and resets only diagnostic receipts between casts.
It records explicit owner state/delta duration and subsequent explicit expiry,
never configured expectations, other actors' states, or a missing duration.
Rejected ability results remain rejected. Five unit tests cover those boundaries.

Initial gate checks RED2failed/5passed1.062s because the route was not yet wired
into the wrapper. After wiring, focused5suites/44tests PASS1.818s includes
observer/gate, Mastery behavior and existing Teleport/Time Warp gate controls.
Bash syntax, changed-file lint, Playwright discovery1test and whitespace pass.
Logs`/tmp/eidolon-fortress-native-qa-{red,green,lint,discovery,final-lint}-20260912.log`.
Browser dependencies are prepared with the ordinary prepare:client command.

No native execution, screenshot acceptance, saved-login proof or full-clear
claim yet. Release65 native predeploy currently owns the GPU; do not compete.
After that gate/final live QA are free, run the scoped route from this committed
worktree using the existing isolated wrapper, retain its artifacts and exact
cleanup result, and correct any demonstrated fixture/runtime failure before
claiming acceptance. Broader party dungeon/earned/first-hour gates stay open.
