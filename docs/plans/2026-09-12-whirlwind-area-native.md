# Whirlwind area native gate — authored, not executed

Parent runtime candidate `876e70ce`. This work adds a dedicated isolated
`whirlwind-area` route after existing Whirlwind duration/movement coverage;
neither this document nor test discovery is native acceptance.

The route uses one allowlisted Fighter and retries zero. A disclosed level100
fixture supplies build points, but all fifteen purchases, specialization,
Extended rune selection, casts and settings use normal phone controls. No
talent assignment, synthetic effect or cooldown reset is used. It tests
Technique0/1/5 and subsequent generic area upgrades (6,6.12,6.6,7.5,8.1),
base/Extended and High/Low graphics. A fresh login checks the saved build,
remaining points and rune, followed by both qualities in landscape.

The serialized observer forwards each original network/effect handler once,
preserves return values/errors, and reads real accepted cast receipts, raw
radius/duration state, effect world-space boundary scale, attachment, visibility,
quality and explicit natural expiry. Reinstalling resets observations without
nesting wrappers. Missing radius/duration is not invented. An accepted correction
must not duplicate the effect. Each cast attaches its receipts/frames; selected
casts request screenshots for subsequent inspection. The previous native
duration/movement route is retained unchanged. This new route does not prove
enemy-hit edge cases or multiplayer observers; server/offline tests cover the
former and public-wire/unit presentation tests cover the latter so far.

Observer five tests pass; before route enrollment, both integration assertions
fail as expected (RED2.359s). After enrollment and both timing-contract arrays
are updated: five suites/125 tests pass5.305s, full lint/bash syntax/diff checks
pass, client vendor preparation succeeds, Playwright lists one test. None of
those commands launches a native browser. Logs:
`/tmp/eidolon-whirlwind-area-native-{red,focused,lint,prepare,list}-20260912.log`.

Production CI34679633154 failed at the unrelated movement-smoothness
sub-arrival check before this route could be run. Diagnose that release failure
first; then execute this frozen route on the single native GPU queue, archive
receipts/images and inspect them before claiming acceptance.
