# Next release integration — local, unversioned acceptance candidate

This branch extends CI-only49daea2e, itself based on socket298ecde0 and canonical
release62dc ancestry. It is not a deployment, a new version assignment or a
completed1.1 milestone. The published62dc candidate must finish its existing
release gate before successor promotion.

## Included changes and evidence boundaries

- Existing socket/equipment appearance, Forge refresh, held-Shift move-only and
  rest-aura work are inherited from socket298ecde0. Its earlier complete native
  all proof remains historical evidence for that source, not automatic acceptance
  of this changed combined source.
- The CI partition preserves every current anonymous/interface case, separate
  artifacts, one worker and all existing predeploy dependencies. It does not
  resolve the independent long-soak runner reservation or prove a speedup.
- Ready-quest guidance0741ff11 and its browser follow-ups are imported as
  34d1de13/e776b4e7/0a7d7911/40568a38. Claimable story/daily quests displace generic
  Recall/Respawn orientation without forcing tracking or automatic completion.
  Desktop shows the existing full hint; phone keeps its compact row and opens
  the readable journal. Source-specific native4case proof remains in the
  [guidance record](2026-09-10-ready-quest-guidance.md).
- Empty-stash runtime d70e5acc is imported as f52f3157: join/resume always sends
  an authoritative array, including empty storage. Exact payload/immutability
  and client stale-display contracts are included. The primary's separate
  fcdfa366 four-class native proof exercised this server path but also included
  its separate starting-stat QA correction; do not claim that entire primary
  branch or its expanded story/balance has been imported here.

On cleanf52f3157,14417 completed343tests/9suites10.299seconds plus lint and exact
CI-mode Playwright discovery103cases with no omissions/duplicates/empty groups.
Job1:22layout/4entrances/3effects/6nameplates/3HUD;
job2:23layout/3entrances/3effects/11interface;
job3:20layout/3entrances/2effects. This includes the two new guidance cases.
46148 passed the actual initial-stash snapshot path under the Go race detector
in1.849seconds. Logs`/tmp/eidolon-next-candidate-{focused,lint,discovery,stash-race}.log`.

## Required authenticated storage regression

The existing required authenticated login/menu/reconnect case now waits for
actual stash replication, records full contents, checks them after the normal
transport-resume exercise, then performs a fresh page login and requires the
same contents again. Missing data is not fabricated as empty; the fresh page
cannot pass by retaining the prior client's array. This adds no inventory action,
grants, account mutation or extra registered account. Existing users may have
nonempty storage; the isolated wrapper's new account covers empty storage.

12484 passed lint and discovered all three existing authenticated cases; the
new checks have NOT yet executed in a browser. Discovery is not smoke acceptance.
Logs`/tmp/eidolon-next-candidate-stash-gate-{lint,discovery}.log`.

## Remaining promotion gates

- [ ] Full combined-source Go race/client regression and lint.
- [ ] Execute authenticated smoke with the strengthened stash checks.
- [ ] Run all three proposed CI-mode bundled-browser groups, retain distinct
  artifacts, inspect rendering and measure real stage durations.
- [ ] Complete the changed versioned candidate's required predeploy/authenticated
  all and other release gates. Do not repeat unchanged old source as missing, or
  substitute its old pass for a changed-source release acceptance claim.
- [ ] Finish canonical62dc acceptance, assign the successor version consistently,
  write accurate player-facing patch notes, run remote CI and verify live
  frontend/backend identities and the affected gameplay after deployment.

The primary earned campaign6251 currently owns the only local heavy/browser
slot on separate frozen9b9dfffe. Keep it running; no parallel heavy/native check
was started for this integration. Its dungeon outcome, all-class/group/realm/raid,
phone/balance and later roadmap acceptance are still open. CI scheduling changes
must not cancel the running soak or redefine its24hour stability requirement
without the separately requested operational decision.
