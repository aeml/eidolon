# Earned Earth functional route — saved chapter stages

The prepared-level replay on419df41 reached the final authored Earth hunt but
hit its enclosing600-second limit. The recorded state shows completed opening,
diary,40Skeletons,8Seeds,60Imps and three scar investigations; The Borrowed Oath
was accepted at19/50DemonOrcs. The screenshot shows active combat, not a blocked
dialogue. Four actual merchant visits passed their individual sale, Gold and
preserved-inventory assertions. This is partial functional evidence, not a full
clear or fresh-player balance result.

Failure archive: `/tmp/eidolon-functional-earth-budget-failure-kMxAKx`;
log: `/tmp/eidolon-earth-merchant-repeat-0002.log`. The wrapper completed artifact
sanitization and disposed of its services. The whole-world failure image was
inspected September10. No rerun of that unchanged single-test budget is needed.

## Change

Split the prepared collection verification into three serial600-second stages:

1. Opening, diary,40Skeletons and naturally dropped8Seeds, with manual claims and
   exact Seed consumption; reload and verify the saved remaining stack.
2. The same account earns60qualifyingImps and all scar investigations; manually
   claim and reload to verify saved completion and the unchanged remaining Seeds.
3. The same account earns50qualifyingDemonOrcs, manually claims, then accepts the
   dungeon chapter while Rootheart remains sealed. Reload verifies the offer and
   unchanged remaining Seeds.

Only the first stage prepares the level. Subsequent stages require real saved
prerequisites; they cannot grant or skip progress. A serial retry uses a distinct
disposable account and replays the entire sequence. The composite helper still
executes all stages in order for the actual dungeon route. Existing encounter
bounds, failure screenshots, ordinary inventory management and manual rewards
remain. Each phase restores the player's previous auto-loot preference.

This does not change runtime content, enemy eligibility,150hunt kills, drop odds,
rewards, progression or any fresh-character/class/group/dungeon completion gate.

## Verification

- [x] Focused prerequisite tests:5passed, including retained eligibility/failure
  behavior and stage wiring; lint and diff whitespace checks passed.
- [x] Full client regression after the refactor and accepted59 integration.
- [ ] Actual three-stage route, saved-state checks and retained terminal evidence.
- [ ] Separate full fresh-character readiness and actual dungeon completion.
